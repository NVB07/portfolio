import MainContent from "@/components/main/ManinContent";
import styles from "./page.module.css";

export default function Home() {
    return (
        <main className={styles.main}>
            <MainContent />
        </main>
    );
}
